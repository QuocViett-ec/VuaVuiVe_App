<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".VuaVuiVeApp"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.VuaVuiVe.Customer"
        android:usesCleartextTraffic="true">

        <meta-data
            android:name="vn.vuavuive.BASE_URL"
            android:value="${vvvBaseUrl}" />
        <meta-data
            android:name="vn.vuavuive.PORTAL_SCOPE"
            android:value="${vvvPortalScope}" />

        <service
            android:name="vn.vuavuive.shared.fcm.VuaVuiVeMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Auth -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.VuaVuiVe.Customer" />

        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".ui.auth.ForgotPasswordActivity"
            android:exported="false" />

        <!-- Main -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="vuavuive"
                    android:host="orders" />
            </intent-filter>
        </activity>

        <!-- Product -->
        <activity
            android:name=".ui.product.ProductDetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.product.FlashSaleActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.search.SearchActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Checkout & Payment -->
        <activity
            android:name=".ui.checkout.CheckoutActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.checkout.MapPickerActivity"
            android:exported="false"
            android:parentActivityName=".ui.checkout.CheckoutActivity" />

        <activity
            android:name=".ui.checkout.PaymentWebViewActivity"
            android:exported="false"
            android:parentActivityName=".ui.checkout.CheckoutActivity" />

        <activity
            android:name=".ui.checkout.PaymentResultActivity"
            android:exported="false"
            android:parentActivityName=".ui.checkout.CheckoutActivity" />

        <!-- Order Detail -->
        <activity
            android:name=".ui.order.OrderDetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Reviews -->
        <activity
            android:name=".ui.review.MyReviewsActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Shipments -->
        <activity
            android:name=".ui.shipment.ShipmentListActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.shipment.ShipmentDetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.shipment.ShipmentListActivity" />

        <!-- Account -->
        <activity
            android:name=".ui.account.EditProfileActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.account.ChangePasswordActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <!-- Chat / Chatbot -->
        <activity
            android:name=".ui.chat.ChatActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity"
            android:windowSoftInputMode="adjustResize" />

        <!-- Recipes -->
        <activity
            android:name=".ui.recipe.RecipeListFragmentActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.recipe.RecipeDetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.recipe.RecipeListFragmentActivity" />

        <!-- ── Shipper App ── -->
    </application>
</manifest>
