<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".VuaVuiVeAdminApp"
        android:allowBackup="true"
        android:label="VVV Admin"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.VuaVuiVe.Admin"
        android:usesCleartextTraffic="true">

        <meta-data
            android:name="vn.vuavuive.BASE_URL"
            android:value="${vvvBaseUrl}" />
        <meta-data
            android:name="vn.vuavuive.PORTAL_SCOPE"
            android:value="${vvvPortalScope}" />

        <service
            android:name="vn.vuavuive.shared.fcm.VuaVuiVeMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity
            android:name=".ui.auth.AdminLoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false" />

        <activity
            android:name=".ui.orders.AdminOrderDetailActivity"
            android:exported="false" />

        <activity
            android:name=".ui.products.ProductEditActivity"
            android:exported="false" />

        <activity
            android:name=".ui.vouchers.VoucherEditActivity"
            android:exported="false" />

        <activity
            android:name=".ui.shipments.ShipmentDetailActivity"
            android:exported="false" />

    </application>
</manifest>
