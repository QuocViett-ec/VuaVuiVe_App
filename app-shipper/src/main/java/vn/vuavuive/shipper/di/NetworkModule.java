package vn.vuavuive.shipper.di;

/**
 * NetworkModule — DEPRECATED (Firebase migration).
 *
 * Module này đã được thay thế bởi ShipperModule.java.
 * Tất cả DI đã chuyển sang Firebase-based providers.
 * File này được giữ lại nhưng không còn cung cấp bất kỳ binding nào.
 *
 * TODO: Xoá file này sau khi verify migration hoàn tất.
 */
// NetworkModule intentionally left empty — all bindings moved to ShipperModule

